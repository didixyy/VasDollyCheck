# 解决GitHub推送问题

## 🔴 错误原因

你的Personal Access Token缺少 `workflow` 权限，无法推送 `.github/workflows/build.yml` 文件。

## ✅ 解决方案（二选一）

### 方案1：重新生成Token（推荐）⭐

1. **访问Token设置页面**：https://github.com/settings/tokens
2. **生成新Token**：点击 "Generate new token" → "Generate new token (classic)"
3. **设置权限**：
   - ✅ 勾选 `repo`（完整仓库访问权限）
   - ✅ 勾选 `workflow`（更新GitHub Actions工作流）
4. **复制Token**
5. **重新推送**：
```bash
cd /Users/yuepeng/Documents/work/channel
git push -u origin main
# 输入用户名和新Token
```

### 方案2：暂时不推送workflow文件

如果暂时不需要GitHub Actions自动构建：

```bash
cd /Users/yuepeng/Documents/work/channel

# 暂时移除workflow文件
mv .github/workflows/build.yml ~/Desktop/build.yml.backup

# 重新提交和推送
git add .
git commit -m "移除workflow文件"
git push -u origin main

# 推送成功后，可以在GitHub网页上手动创建workflow文件
```

## 🎯 推荐做法

**使用方案1**，这样你可以：
- ✅ 自动构建Windows和macOS版本
- ✅ 每次推送代码自动触发构建
- ✅ 创建Release时自动打包

只需要在生成Token时多勾选一个 `workflow` 权限即可！

## 📝 正确的Token权限设置

访问 https://github.com/settings/tokens 创建新token时：

```
Token名称: VasDollyCheck Deploy
过期时间: 选择一个合适的时间（如90天）

权限（Scopes）：
☑️ repo
   ☑️ repo:status
   ☑️ repo_deployment
   ☑️ public_repo
   ☑️ repo:invite
   ☑️ security_events
☑️ workflow  ← 重要！这个权限是必须的
```

生成后复制Token，然后重新推送即可！

